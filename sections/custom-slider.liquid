{% schema %}
{
  "name": "Custom Slider",
  "settings": [],
  "blocks": [
    {
      "type": "image_and_description",
      "name": "Slide",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Slide Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Slide Title"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Slide Description"
        }
      ]
    }
  ]
}
{% endschema %}


<!-- custom-slider.liquid -->
<div class="carousel-container">
  <div class="carousel-wrapper">
    {% for slide in section.blocks %}
      <div class="carousel-slide">
        <!-- Customize the content for each slide -->
        <img src="{{ slide.settings.image | img_url: 'medium' }}" alt="{{ slide.settings.title }}">
        <h3>{{ slide.settings.title }}</h3>
        <p>{{ slide.settings.description }}</p>
      </div>
    {% endfor %}
  </div>

  <button class="arrow left-arrow">&lt;</button>
  <button class="arrow right-arrow">&gt;</button>
</div>
